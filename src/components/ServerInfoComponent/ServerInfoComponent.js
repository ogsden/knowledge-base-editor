import React from "react";

const ServerInfoComponent = () => {
  return (
    <div className="server-info">
      Информация с сервера (будет отображаться после выполнения определенных действий пользователя)
    </div>
  );
};

export default ServerInfoComponent;
